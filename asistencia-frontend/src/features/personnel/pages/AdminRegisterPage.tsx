import { AdminRegistration } from '../components/AdminRegistration' // Sección de registro (existente/nuevo)

// Página de registro dentro del dashboard. // UI
export function AdminRegisterPage() {
  return <AdminRegistration /> // Renderiza el módulo solicitado
} // Fin AdminRegisterPage

